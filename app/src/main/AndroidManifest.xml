<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScoreTracker">
        <activity
            android:name="fr.corentin.roux.x_wing_score_tracker.ui.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ScoreTracker.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="fr.corentin.roux.x_wing_score_tracker.ui.activities.TimerActivity"
            android:theme="@style/Theme.ScoreTracker.NoActionBar"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="fr.corentin.roux.x_wing_score_tracker.ui.activities.HistoriqueActivity"
            android:theme="@style/Theme.ScoreTracker.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="fr.corentin.roux.x_wing_score_tracker.ui.activities.HistoriqueDetailActivity"
            android:theme="@style/Theme.ScoreTracker.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="fr.corentin.roux.x_wing_score_tracker.ui.activities.MissionDetailActivity"
            android:theme="@style/Theme.ScoreTracker.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:theme="@style/Theme.ScoreTracker.NoActionBar"
            android:name="fr.corentin.roux.x_wing_score_tracker.ui.activities.SettingsActivity"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>